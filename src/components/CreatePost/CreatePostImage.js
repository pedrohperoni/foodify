import styled from "styled-components";

const CreatePostImage = styled.img`
  margin: 10px 0;
  width: auto;
  border-radius: 10px;
  object-fit: cover;
  max-height: 400px;
  max-width: 100%;
  border-radius: 10px;
`;

export default CreatePostImage;
